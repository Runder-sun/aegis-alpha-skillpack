# pipeline Scenarios

- List workflows: `pipeline-list`.
- Validate nightly without side effects: `pipeline-dry-run` with `pipeline_id=nightly`.
- Run weekly without push: `pipeline-run-weekly` with `allow_push=false`.
