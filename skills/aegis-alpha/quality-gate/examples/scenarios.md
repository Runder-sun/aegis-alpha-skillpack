# quality-gate Examples

## nightly-quality-gate
- Trigger: `/ nightly-quality-gate`
- Invocation: `python scripts/dispatch.py --command nightly-quality-gate --payload '{}'`

## backtest-loop
- Trigger: `/ backtest-loop`
- Invocation: `python scripts/dispatch.py --command backtest-loop --payload '{}'`
