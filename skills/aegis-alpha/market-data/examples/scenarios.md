# Scenarios

## Nightly pipeline usage

- `snapshot-full` provides the consolidated HHXG snapshot for nightly strategy.
- `macro-pmi` / `macro-cpi` / `macro-ppi` provide macro series for nightly summaries.
- `index-daily` provides index daily history for nightly summaries.
