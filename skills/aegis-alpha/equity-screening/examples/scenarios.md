# equity-screening Examples

## board-universe-sync
- Trigger: `/ board-universe-sync`
- Invocation: `python scripts/dispatch.py --command board-universe-sync --payload '{}'`

## company-evidence-collect
- Trigger: `/ company-evidence-collect`
- Invocation: `python scripts/dispatch.py --command company-evidence-collect --payload '{}'`

## layered-stock-screening
- Trigger: `/ layered-stock-screening`
- Invocation: `python scripts/dispatch.py --command layered-stock-screening --payload '{}'`

## leader-source-harvest
- Trigger: `/ leader-source-harvest`
- Invocation: `python scripts/dispatch.py --command leader-source-harvest --payload '{}'`

## stock-pool-incremental-am
- Trigger: `/ stock-pool-incremental-am`
- Invocation: `python scripts/dispatch.py --command stock-pool-incremental-am --payload '{}'`

## stock-pool-incremental-pm
- Trigger: `/ stock-pool-incremental-pm`
- Invocation: `python scripts/dispatch.py --command stock-pool-incremental-pm --payload '{}'`

## stock-pool-maintenance
- Trigger: `/ stock-pool-maintenance`
- Invocation: `python scripts/dispatch.py --command stock-pool-maintenance --payload '{}'`

## stock-rating
- Trigger: `/ stock-rating`
- Invocation: `python scripts/dispatch.py --command stock-rating --payload '{}'`

## stock-screening
- Trigger: `/ stock-screening`
- Invocation: `python scripts/dispatch.py --command stock-screening --payload '{}'`

## stock-screening-v2
- Trigger: `/ stock-screening-v2`
- Invocation: `python scripts/dispatch.py --command stock-screening-v2 --payload '{}'`
