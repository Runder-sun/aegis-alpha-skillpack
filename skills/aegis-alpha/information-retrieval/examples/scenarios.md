# information-retrieval Scenarios

- Search current evidence: `research-search` with `query` or `queries`.
- Fetch a known article: `fetch-content` with `url`.
- Parse a PDF or document: `parse-document` with `paths` or `urls`.
- Review prior artifacts: `research-history`.
