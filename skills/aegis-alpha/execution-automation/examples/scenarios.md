# execution-automation Examples

## evening-prewarm
- Trigger: `/ evening-prewarm`
- Invocation: `python scripts/dispatch.py --command evening-prewarm --payload '{}'`

## intraday-push
- Trigger: `/ intraday-push`
- Invocation: `python scripts/dispatch.py --command intraday-push --payload '{}'`

## market-heartbeat
- Trigger: `/ market-heartbeat`
- Invocation: `python scripts/dispatch.py --command market-heartbeat --payload '{}'`

## midday-prewarm
- Trigger: `/ midday-prewarm`
- Invocation: `python scripts/dispatch.py --command midday-prewarm --payload '{}'`

## morning-prewarm
- Trigger: `/ morning-prewarm`
- Invocation: `python scripts/dispatch.py --command morning-prewarm --payload '{}'`

## morning-push
- Trigger: `/ morning-push`
- Invocation: `python scripts/dispatch.py --command morning-push --payload '{}'`

## nightly-prewarm
- Trigger: `/ nightly-prewarm`
- Invocation: `python scripts/dispatch.py --command nightly-prewarm --payload '{}'`

## nightly-push
- Trigger: `/ nightly-push`
- Invocation: `python scripts/dispatch.py --command nightly-push --payload '{}'`

## prewarm-status
- Trigger: `/ prewarm-status`
- Invocation: `python scripts/dispatch.py --command prewarm-status --payload '{}'`

## realtime-alerts-summary
- Trigger: `/ realtime-alerts-summary`
- Invocation: `python scripts/dispatch.py --command realtime-alerts-summary --payload '{}'`

## realtime-market-scan
- Trigger: `/ realtime-market-scan`
- Invocation: `python scripts/dispatch.py --command realtime-market-scan --payload '{}'`

## realtime-monitor-control
- Trigger: `/ realtime-monitor-control`
- Invocation: `python scripts/dispatch.py --command realtime-monitor-control --payload '{}'`

## weekly-prewarm
- Trigger: `/ weekly-prewarm`
- Invocation: `python scripts/dispatch.py --command weekly-prewarm --payload '{}'`

## weekly-push
- Trigger: `/ weekly-push`
- Invocation: `python scripts/dispatch.py --command weekly-push --payload '{}'`
