# advice-lifecycle Examples

## advice-expire-check
- Trigger: `/ advice-expire-check`
- Invocation: `python scripts/dispatch.py --command advice-expire-check --payload '{}'`

## advice-history
- Trigger: `/ advice-history`
- Invocation: `python scripts/dispatch.py --command advice-history --payload '{}'`

## advice-track-report
- Trigger: `/ advice-track-report`
- Invocation: `python scripts/dispatch.py --command advice-track-report --payload '{}'`

## advice-track-stats
- Trigger: `/ advice-track-stats`
- Invocation: `python scripts/dispatch.py --command advice-track-stats --payload '{}'`

## advice-update-prices
- Trigger: `/ advice-update-prices`
- Invocation: `python scripts/dispatch.py --command advice-update-prices --payload '{}'`

## investment-advice
- Trigger: `/ investment-advice`
- Invocation: `python scripts/dispatch.py --command investment-advice --payload '{}'`

## morning-briefing
- Trigger: `/ morning-briefing`
- Invocation: `python scripts/dispatch.py --command morning-briefing --payload '{}'`

## nightly-strategy
- Trigger: `/ nightly-strategy`
- Invocation: `python scripts/dispatch.py --command nightly-strategy --payload '{}'`

## update-daily-advice
- Trigger: `/ update-daily-advice`
- Invocation: `python scripts/dispatch.py --command update-daily-advice --payload '{}'`

## weekly-asset-report
- Trigger: `/ weekly-asset-report`
- Invocation: `python scripts/dispatch.py --command weekly-asset-report --payload '{}'`
